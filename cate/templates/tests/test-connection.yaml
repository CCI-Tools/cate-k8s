apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cate-webui.fullname" . }}-test-connection"
  labels:
    {{- include "cate-webui.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cate-webui.fullname" . }}:{{ .Values.webui.service.port }}']
  restartPolicy: Never
